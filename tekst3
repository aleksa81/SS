ORG 0
.text.1
a:DD 12 DUP ?
b:DD ?
JMP $k                 ; pc rel - PC iz ORG, k iz ORG
JMP $c                 ; pc rel - PC iz ORG, c nije iz ORG + c je globalan
JMP $h                 ; pc rel - PC iz ORG, h nije iz ORG + h je lokalan
end1:

ORG end1
.text.2
k:DW 48 DUP 4
JMP $1234              ; pc rel - apsolutna adresa u ORG sekciji

.global k,c,d

.text.3
c:DD 3
JMP $a                 ; pc rel - PC nije iz ORG, a je iz ORG + a je lokalan
e:JMP $1234            ; pc rel - apsolutna adresa u ne ORG sekciji
JMP $c                 ; pc rel - PC i c iz iste sekcije

.text.5
ADD R1, R2, R3
JMP $c                 ; pc rel - PC nije iz ORG, c nije iz ORG + c je globalan
JMP $e                 ; pc rel - PC nije iz ORG, e nije iz ORG + e je lokalan
JZ R1, k               ; mem dir - k iz ORG + k je globlan
h:JZ R1, c             ; mem dir - c nije iz ORG + c je globalan
JZ R1, a               ; mem dir - a iz ORG + a je lokalan
JZ R1, e               ; mem dir - e nije iz ORG + e je lokalan

.end